class Solution {
  public:
    void reverseArray(vector<int> &arr) {
        // code here
        int temp;
        for (int i=0,j=arr.size()-1;i<j;i++,j--)
        {
            swap(arr[i],arr[j]);
        }
    }
};
